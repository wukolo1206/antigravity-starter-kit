# 🔑 Google API OAuth 用戶端金鑰放置說明

這個資料夾是專門用來存放 Google API 授權憑證的。請遵循以下步驟來完成您的 Google APIs 連線設定：

## 🛠️ 第一步：取得憑證
1. 前往 Google Cloud Console (https://console.cloud.google.com/)。
2. 建立新專案，並在 API 庫中啟用：
   - Google Drive API
   - Google Slides API
   - Google Forms API
   - Google Sheets API
3. 至「OAuth 同意畫面」設定為「外部」，並將您個人的 Google 帳號新增至「測試使用者 (Test users)」中。
4. 前往「憑證」➔ 點選「建立憑證」➔ 選擇「OAuth 用戶端識別碼」➔ 類型選「電腦版應用程式」。
5. 下載產出的 JSON 憑證檔案。

## 📁 第二步：放置並更名
1. 將下載的 `.json` 憑證檔案移動到此 `google workspace/` 資料夾下。
2. 將檔案名稱重新命名為：
   👉 **`credentials.json`** （請確保副檔名是 .json，不可為 credentials.json.json 或其他名稱）。

## 💡 研習現場快捷提示
如果是研習現場，全班可以共用講師隨身碟中提供的同一個 `credentials.json` 檔案。
直接將其複製並貼存到此 `google workspace/` 目錄下即可！
